mcp>=1.2.1
starlette>=0.36.0
uvicorn>=0.27.0
ollama>=0.1.7
chromadb>=0.4.24
Pillow>=10.2.0
watchdog>=4.0.0
psutil>=5.9.8
pymupdf>=1.23.22
openpyxl>=3.1.2
python-docx>=1.1.0
python-pptx>=0.6.23
urllib3>=2.0.0
charset-normalizer>=3.0.0

# --- Search Engine Dependencies ---
# rank_bm25 for in-memory fast keyword search (BM25 algorithm)
rank_bm25>=0.2.2
# flashrank for ultra-lightweight re-ranking based on ONNX runtime
flashrank>=0.2.12

# --- OCR Dependencies (Optional) ---
# By default, Ollama OCR is used (requires Ollama server with glm-ocr model).
# PaddleOCR is optional and can be installed separately if needed.
#
# To use PaddleOCR instead of Ollama:
# 1. Install PaddleOCR dependencies:
#    pip install paddleocr>=2.7.3 paddlepaddle>=2.6.0
#    (For GPU support: pip install paddleocr>=2.7.3 paddlepaddle-gpu>=2.6.0)
# 2. Set "ocr_engine": "paddleocr" in config.json
#
# Note: numpy>=1.23.2 is required for PaddleOCR (usually installed as dependency)
# Note: Users must manually install the appropriate PaddlePaddle backend.
